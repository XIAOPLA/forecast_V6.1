# V6.1 方法库精简改动报告

## 一、改动目标

本次改动的目标是降低 V6.1 方法库复杂度，优先剔除贡献弱、功能重叠或稳定性不足的方法，同时保留后续继续做消融实验和专项恢复的空间。

改动遵循两个原则：

1. 对证据较充分、保留价值较低的方法，直接从主方法库删除。
2. 对仍有局部价值但不适合继续作为主候选的方法，暂时移出主方法库，保留实现以便后续专项实验。

## 二、方法处理结果

### 1. 直接删除的方法

| 方法 | 处理 | 理由 |
| --- | --- | --- |
| `ma_7` | 从主方法库删除 | 胜出次数仅 8 次，覆盖模式窄，删除 `damped_trend + ma_7` 的消融中 MASE 仅上升 0.001002 |
| `holt_winters` | 从主方法库删除 | 季节趋势组中无明显优势，平均 MASE 高于 `seasonal_naive`、`optimized_des`、`optimized_hw` |

### 2. 暂时移出主方法库的方法

| 方法 | 处理 | 理由 |
| --- | --- | --- |
| `sba` | 保留实现，移出主候选池 | 间歇需求组内被 `tsb_opt` 明显压制，但与 `croston/adida` 对比仍有一定价值，暂不物理删除 |
| `naive_drift` | 保留实现，移出主候选池 | 平均排名靠后，稳定性不足；直接删除会带来一定 MASE 损失，因此保留实现便于后续限制性实验 |
| `damped_trend` | 保留实现，移出主候选池 | 胜出次数少，仅 16 个物料；与 `ma_7` 联合删除影响较小，但作为趋势类备选仍保留实现 |

### 3. 当前主方法库保留方法

当前主候选池保留 14 个方法：

`ma_3`、`ma_5`、`optimized_ses`、`optimized_des`、`seasonal_naive`、`tsb_opt`、`median_5`、`interval_based`、`croston`、`adida`、`optimized_hw`、`theta`、`weighted_ma_5`、`winsorized`

## 三、关键数据依据

### 1. `ma_7 + damped_trend` 消融结果

历史消融结果显示，同时禁用 `damped_trend + ma_7` 后：

| 指标 | 完整方法库 | 禁用后 | 变化 |
| --- | ---: | ---: | ---: |
| 平均 MASE | 0.181751 | 0.182753 | +0.001002 |
| MASE 相对变化 | - | - | +0.5512% |
| 平均总量误差 | 20.158459 | 20.683406 | +0.524947 |
| 总量误差相对变化 | - | - | +2.6041% |

`damped_trend` 原胜出 16 个物料，`ma_7` 原胜出 8 个物料，合计 24 个物料。该实验说明这两个方法对整体 MASE 的边际贡献较小。

### 2. `holt_winters` 季节趋势组对比

季节趋势方法组内 420 次共同比较结果：

| 方法 | 组内第一次数 | 组内第一率 | 组内平均 MASE | 组内平均排名 |
| --- | ---: | ---: | ---: | ---: |
| `seasonal_naive` | 132 | 31.43% | 0.9172 | 2.46 |
| `holt_winters` | 98 | 23.33% | 1.0026 | 2.57 |
| `optimized_des` | 96 | 22.86% | 0.8767 | 2.45 |
| `optimized_hw` | 94 | 22.38% | 0.9319 | 2.52 |

两两对比中，`holt_winters` 均略弱：

| 对比 | `holt_winters` 更优率 | 对方更优率 |
| --- | ---: | ---: |
| `holt_winters` vs `seasonal_naive` | 46.19% | 53.10% |
| `holt_winters` vs `optimized_des` | 46.19% | 52.14% |
| `holt_winters` vs `optimized_hw` | 47.62% | 51.43% |

因此 `holt_winters` 不具备明显保留优势。

### 3. `sba` 间歇需求组对比

间歇需求方法组内 750 次共同比较结果：

| 方法 | 组内第一次数 | 组内第一率 | 组内平均 MASE | 组内平均排名 |
| --- | ---: | ---: | ---: | ---: |
| `tsb_opt` | 355 | 47.33% | 0.5406 | 2.21 |
| `adida` | 145 | 19.33% | 0.6535 | 2.61 |
| `croston` | 125 | 16.67% | 0.6638 | 2.69 |
| `sba` | 125 | 16.67% | 0.6478 | 2.49 |

`sba` 明显弱于 `tsb_opt`，但并不明显弱于 `croston/adida`。因此本次不直接删除 `sba`，而是暂时移出主候选池，保留函数实现。

### 4. `naive_drift + damped_trend` 消融结果

禁用 `naive_drift + damped_trend` 后：

| 指标 | 完整方法库 | 禁用后 | 变化 |
| --- | ---: | ---: | ---: |
| 平均 MASE | 0.1818 | 0.1951 | +0.0133 |
| 平均总量误差 | 20.1585 | 21.3609 | +1.2025 |

该结果说明 `naive_drift` 和 `damped_trend` 并非完全无贡献，因此本次选择“暂时移出主方法库”，而不是物理删除。

## 四、改动后运行结果

本次精简后重新运行 V6.1，结果如下：

| 指标 | 精简后结果 |
| --- | ---: |
| 物料数 | 1239 |
| all_zero 物料 | 69 |
| 平均 MASE | 0.2014 |
| 平均总量误差 | 23.3634 |
| 相对移动平均 MASE 提升 | 68.35% |
| 相对移动平均总量误差提升 | 66.66% |
| V6 新增方法采用数 | 386 |

与精简前完整方法库相比：

| 指标 | 精简前 | 精简后 | 变化 |
| --- | ---: | ---: | ---: |
| 平均 MASE | 0.1818 | 0.2014 | +0.0196 |
| 平均总量误差 | 20.1585 | 23.3634 | +3.2049 |
| V6 新增方法采用数 | 394 | 386 | -8 |

精简带来了模型库简化，但也造成一定预测性能损失。该损失主要来自将 `sba`、`naive_drift`、`damped_trend` 暂时移出主候选池。

## 五、结论

本次改动适合作为“精简主方法库”的第一版方案：

1. `ma_7` 和 `holt_winters` 可以删除，数据支持较充分。
2. `sba`、`naive_drift`、`damped_trend` 不建议物理删除，只建议暂时移出主候选池。
3. 精简后方法库更清晰，但平均 MASE 从 0.1818 上升到 0.2014，存在可见性能损失。
4. 下一步建议针对 `sba`、`naive_drift`、`damped_trend` 做限制性恢复实验，例如只在特定模式中启用，而不是全局恢复。

